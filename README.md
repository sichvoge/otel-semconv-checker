# otel-semconv-checker
OTel SemConv Checker is a self-hosted web application that turns weaver registry live-check output into a human-readable conformance report, showing exactly what your OTLP instrumentation emits at runtime that breaks the spec, and what it's missing, without having to parse raw JSON.
